<Page x:Class="UchebnPractica941Vesna.Pages.RegistrationPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" d:DesignWidth="500"
      Title="RegistrationPage"
      FontFamily="Candara"
      Background="White" Height="1069">

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <Grid Width="400" Margin="0,20" Height="1137">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

           
            <Border  Grid.Row="0" Padding="20" Margin="0,0,0,20" CornerRadius="5">
                <StackPanel>

                    <TextBlock Text="Регистрация нового пользователя" FontSize="16" 
                               Foreground="#355CBD" HorizontalAlignment="Center" 
                               Margin="0,10,0,0"/>
                </StackPanel>
            </Border>

           
            <StackPanel Grid.Row="1" Margin="0,8">
                <TextBlock Text="Фамилия" Margin="0,0,0,5" FontSize="14" FontWeight="SemiBold"/>
                <TextBox x:Name="SurnameBox" Padding="10" FontSize="14" Background="#F9F9F9"/>
            </StackPanel>

            
            <StackPanel Grid.Row="2" Margin="0,8">
                <TextBlock Text="Имя" Margin="0,0,0,5" FontSize="14" FontWeight="SemiBold"/>
                <TextBox x:Name="NameBox" Padding="10" FontSize="14" Background="#F9F9F9"/>
            </StackPanel>

           
            <StackPanel Grid.Row="3" Margin="0,8">
                <TextBlock Text="Отчество" Margin="0,0,0,5" FontSize="14" FontWeight="SemiBold"/>
                <TextBox x:Name="PatronumicBox" Padding="10" FontSize="14" Background="#F9F9F9"/>
            </StackPanel>

            <StackPanel Grid.Row="4" Margin="0,8">
                <TextBlock Text="Дата рождения" Margin="0,0,0,5" FontSize="14" FontWeight="SemiBold"/>
                <DatePicker x:Name="BirthdayPicker" Height="40" Background="#F9F9F9"/>
            </StackPanel>

           
            <StackPanel Grid.Row="5" Margin="0,8">
                <TextBlock Text="Логин" Margin="0,0,0,5" FontSize="14" FontWeight="SemiBold"/>
                <TextBox x:Name="LoginBox" Padding="10" FontSize="14" Background="#F9F9F9"/>
            </StackPanel>

            
            <StackPanel Grid.Row="6" Margin="0,8">
                <TextBlock Text="Пароль" Margin="0,0,0,5" FontSize="14" FontWeight="SemiBold"/>
                <PasswordBox x:Name="PasswordBox" Padding="10" FontSize="14" Background="#F9F9F9"/>
            </StackPanel>

           
            <StackPanel Grid.Row="7" Margin="0,8">
                <TextBlock Text="Подтверждение пароля" Margin="0,0,0,5" FontSize="14" FontWeight="SemiBold"/>
                <PasswordBox x:Name="ConfirmPasswordBox" Padding="10" FontSize="14" Background="#F9F9F9"/>
            </StackPanel>

            <StackPanel Grid.Row="8" Margin="0,8">
                <TextBlock Text="Роль" Margin="0,0,0,5" FontSize="14" FontWeight="SemiBold"/>
                <ComboBox x:Name="RoleCmb" Padding="10" FontSize="14" Height="40" Background="#F9F9F9"/>
            </StackPanel>

            <StackPanel Grid.Row="9" Orientation="Horizontal" 
                        HorizontalAlignment="Center" Margin="0,30">
                <Button x:Name="RegisterBtn" Click="RegisterBtn_Click" 
                        Background="#355CBD" Foreground="White" 
                        Content="Зарегистрироваться" FontSize="14" 
                        Width="180" Height="40" Margin="5" Cursor="Hand"/>
                <Button x:Name="CancelBtn" Click="CancelBtn_Click" 
                        Background="White" Foreground="#355CBD" 
                        Content="Отмена" FontSize="14" 
                        Width="100" Height="40" Margin="5"
                        BorderBrush="#355CBD" BorderThickness="1" Cursor="Hand"/>
            </StackPanel>

           
            <TextBlock x:Name="ErrorText" Grid.Row="10" 
                       Foreground="Red" TextWrapping="Wrap" 
                       HorizontalAlignment="Center" Margin="0,10"
                       FontSize="12"/>
        </Grid>
    </ScrollViewer>
</Page>