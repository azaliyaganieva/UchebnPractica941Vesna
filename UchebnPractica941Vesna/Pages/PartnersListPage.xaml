<Page x:Class="UchebnPractica941Vesna.Pages.PartnersListPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="800"
      Title="PartnersListPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

     
        <StackPanel Grid.Row="0" Margin="10" Orientation="Horizontal" HorizontalAlignment="Center">
            <TextBlock Text="Поиск:" VerticalAlignment="Center" Margin="5"/>
            <TextBox x:Name="SearchBox" Width="200" Margin="5" 
                     TextChanged="SearchBox_TextChanged"/>
        </StackPanel>
 
        <StackPanel Grid.Row="1" Margin="10" Orientation="Horizontal" HorizontalAlignment="Center">
            <TextBlock Text="Сортировать по:" VerticalAlignment="Center" Margin="5"/>
            <ComboBox x:Name="SortCombo" Width="150" Margin="5" 
                      SelectionChanged="SortCombo_SelectionChanged">
                <ComboBoxItem Content="Наименованию (А-Я)"/>
                <ComboBoxItem Content="Наименованию (Я-А)"/>
                <ComboBoxItem Content="Рейтингу (возрастание)"/>
                <ComboBoxItem Content="Рейтингу (убывание)"/>
                <ComboBoxItem Content="Скидке (возрастание)"/>
                <ComboBoxItem Content="Скидке (убывание)"/>
            </ComboBox>
        </StackPanel>

     
        <ListView x:Name="PatnersLW" Grid.Row="2" FontSize="20">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="Black" BorderThickness="2" Margin="10">
                        <Grid Margin="10" Width="700">
                            <Grid.RowDefinitions>
                                <RowDefinition/>
                                <RowDefinition/>
                                <RowDefinition/>
                                <RowDefinition/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition/>
                                <ColumnDefinition/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Row="0" Orientation="Horizontal">
                                <TextBlock Text="{Binding TypeOfBusiness.NameBusiness}"/>
                                <TextBlock Text=" | "/>
                                <TextBlock Text="{Binding NamePartner}"/>
                            </StackPanel>
                            <StackPanel Grid.Row="1" Orientation="Horizontal">
                                <TextBlock Text="{Binding SurnameDirector}"/>
                                <TextBlock Text=" "/>
                                <TextBlock Text="{Binding NameDirector}"/>
                                <TextBlock Text=" "/>
                                <TextBlock Text="{Binding PatronumicDirector}"/>
                            </StackPanel>
                            <TextBlock Grid.Row="2" Text="{Binding Phone}"/>
                            <TextBlock Grid.Row="3" Text="{Binding Raiting}"/>
                            <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding SizeDiscount}" HorizontalAlignment="Right"/>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>


        <StackPanel Orientation="Horizontal" Grid.Row="3" HorizontalAlignment="Center" Margin="10">
            <Button x:Name="AddBtn" Click="AddBtn_Click" Width="100" Height="35" Margin="5">Добавить</Button>
            <Button x:Name="EditBtn" Click="EditBtn_Click" Width="100" Height="35" Margin="5">Изменить</Button>
            <Button x:Name="DeleteBtn" Click="DeleteBtn_Click" Width="100" Height="35" Margin="5" Background="#FF6B6B" Foreground="White">Удалить</Button>
            <Button x:Name="HistoryBtn" Click="HistoryBtn_Click" Width="163" Height="35" Margin="5">История продаж</Button>
            <Button x:Name="RefreshBtn" Click="RefreshBtn_Click" Width="100" Height="35" Margin="5">Обновить</Button>
        </StackPanel>
    </Grid>
</Page>